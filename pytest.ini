[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py

# 注册自定义 marker
markers =
    real_llm: 需要真实 LLM API，会消耗 token（默认 pytest 跳过）

# 默认跳过真实 LLM 测试（节省成本）
# 跑真实测试: pytest -m real_llm
addopts = -m "not real_llm"